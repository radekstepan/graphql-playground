{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "6.0.1"
  },
  "scripts": {
    "start": "yarn lerna run start",
    "deploy": "yarn lerna run build --scope=client"
  }
}
